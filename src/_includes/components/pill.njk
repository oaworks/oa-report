{# Pill component
{{ component("pill", {
  id: "",
  txt: ""
}) }}
#}

<a href="{{ params.csv }}"
  download
  class="inline-flex px-3 py-1 rounded-full bg-white text-xs text-neutral-900 transition duration-300 ease-in-out hover:bg-carnation-500 {% if params.highlight %}bg-carnation-300 {% endif %}js_export_pill" 
  data-title="{{ params.title }}" 
  data-filter-by="{{ params.filter_by }}"
  data-group-by="{{ params.group_by }}"
  data-csv="{{ params.csv }}"
  id="export_{{ params.id }}">
    {{ params.txt }}
</a>