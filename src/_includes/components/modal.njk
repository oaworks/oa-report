{# Modal component
{{ component("modal", {
  id: "",
  title: "",
  export: false
}) }}
#}

<div id="modal_title_{{ params.id }}" class="hidden">Export: {{ params.title }}</div>
<div id="modal_content_{{ params.id }}" class="hidden">
  {% if params.include %}
    {% include 'partials/' + params.include + '.njk' %}
  {% else %}
    {{ params.content|safe }}
  {% endif %}
</div>