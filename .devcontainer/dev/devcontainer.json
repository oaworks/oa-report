{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "postCreateCommand": "export ELEVENTY_API_ENDPOINT=beta && export ELEVENTY_BASE_URL=https://dev.oa.report && export ELEVENTY_NUMBER_REPORTS=20 && npm install && npm run start",
  "containerEnv": {
    "ELEVENTY_API_ENDPOINT": "beta",
    "ELEVENTY_BASE_URL": "https://dev.oa.report",
    "ELEVENTY_NUMBER_REPORTS": "20"
  }
}
