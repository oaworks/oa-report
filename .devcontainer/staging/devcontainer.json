{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "postCreateCommand": "export ELEVENTY_API_ENDPOINT=api && export ELEVENTY_BASE_URL=https://oa.report && export ELEVENTY_NUMBER_REPORTS=100 && npm install && npm run start",
  "containerEnv": {
    "ELEVENTY_API_ENDPOINT": "api",
    "ELEVENTY_BASE_URL": "https://oa.report",
    "ELEVENTY_NUMBER_REPORTS": "100"
  }
}
